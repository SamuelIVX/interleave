package dev.samhb.interleave.format.dsl;

/**
 * Field type for declarative state.
 */
public enum FieldType {
    /** 32-bit signed integer. */
    INT,
    /** Boolean. */
    BOOL,
    /** Array of integers with fixed length. */
    INT_ARRAY
}
